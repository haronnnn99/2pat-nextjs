# Copy to .env (or export inline) before running migrate / deploy.
# Get a token from https://www.sanity.io/manage/personal/tokens
# Migration needs `editor` or higher; deploy to sanity.studio needs `administrator`.
SANITY_AUTH_TOKEN=
